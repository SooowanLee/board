spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true

  #?????? ??
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/board?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: "root"
    password: "0000"

  # Run? cache?? ???? ???? ?? (???? ??? ?? ??)
  thymeleaf:
    cache: false

  # JPA ?? ??, datasource: ??? ?? ??? ??????.
  # JPA? ?????? ??
  jpa:
    hibernate:
      ddl-auto: create # ?????? ?? ? ???? ??? ?? ? ??? ???? ?? ??
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect # MySQL ??? ?? Hibernate ?? ??

